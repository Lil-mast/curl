# Maktab AI — Python dependencies
# Install with: pip install -r requirements.txt
# Python 3.11+ required

# ── ElevenLabs voice service (Isaac — services/voice/) ──────────────────────
elevenlabs>=1.54.0          # STT (scribe_v2) + TTS (eleven_v3)
httpx>=0.27.0               # HTTP client used by ElevenLabs SDK; pinned for timeout handling
python-dotenv>=1.0.0        # Load .env file for local development

# ── Flask API (Alvine — services/api/) ──────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── Database (Faith — services/api/) ────────────────────────────────────────
psycopg[binary]>=3.2.0      # PostgreSQL adapter used by services.api.db
psycopg-pool>=3.2.0         # Connection pool for runtime queries

# ── Flask HTTP (Render) ─────────────────────────────────────────────────────
gunicorn>=22.0.0
